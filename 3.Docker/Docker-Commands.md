docker run nginx 
docker ps
dokcer ps -a
docker stop "container-name"
docker rm "container-name"

docker images
docker rmi nginx
docker pull nginx

<!-- docker run ubuntu
docker ps
    sees nothing
docker ps -a -->

docker run ubuntu sleep 5